extends layout

block content
  table(class='table table-hover')
    each obj in subjectTypes
      tr(data-toggle='collapse' data-target='#{obj.id}' class='accordion-toggle')
        td #{obj.name}
        td #{obj.dailyTotal} / #{obj.dailyGoal}
    else
      tr
        td No subjects found
